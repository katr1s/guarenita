<nav>
  <div class="logotype">
    <a href="/">Guanerita</a>
  </div>

  <!-- El panel completo -->
  <div class="user" id="nav-menu">
    
    <!-- LADO DE LA IMAGEN: Entra desde la izquierda -->
    <div class="menu-graphic-panel">
      <img src="./poster_navbar.webp" alt="Proceso Artesanal" class="panel-img" />
      <div class="vignette"></div>
      <div class="panel-badge">Est. 2026</div>
    </div>

    <!-- LADO DEL TEXTO: Entra desde la derecha -->
    <div class="menu-content-panel">
      <!-- Botón de cerrar integrado directamente en el panel móvil -->
      <div class="panel-header-mobile">
        <span>Menú de Navegación</span>
      </div>

      <div class="links-container">
        <a href="/Productos" class="nav-link-premium" data-text="Productos">Productos</a>
        <a href="/About" class="nav-link-premium" data-text="Acerca">Acerca</a>
        <a href="/Contact" class="nav-link-premium" data-text="Contáctanos">Contáctanos</a>

        <div class="divider-line"></div>

        <div class="nav-actions-premium">
          <a href="/Shopping" class="cart-icon-premium">
            <img src="./shopping.svg" alt="Mis compras" />
            <span class="cart-text">Ver Carrito</span>
          </a>
          <a href="/Account/Login" class="btn-gold-outline">Iniciar sesión</a>
        </div>
      </div>

      <div class="panel-footer-premium">
        <span>Cerveza Independiente</span>
        <span>Santander, Col.</span>
      </div>
    </div>

  </div>

  <button class="menu-trigger" id="menu-toggle" aria-label="Menú">
    <span class="bar"></span>
    <span class="bar"></span>
  </button>
</nav>

<script>
  const menuToggle = document.getElementById('menu-toggle');
  const navMenu = document.getElementById('nav-menu');

  menuToggle.addEventListener('click', () => {
    const isActive = navMenu.classList.toggle('active');
    menuToggle.classList.toggle('open');

    // Bloqueo estricto del scroll
    document.body.style.overflow = isActive ? 'hidden' : '';
  });
</script>

<style>
  /* ==========================================================================
     BASE Y NAV MINIMALISTA
     ========================================================================== */
  a {
    text-decoration: none;
  }

  nav {
    width: 95%;
    height: 80px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin: 0 auto;
    position: relative;
    z-index: 100;
  }

  .logotype {
    z-index: 110;
  }

  .logotype a {
    color: var(--dorado-guanerita, #d9c5a0);
    font-size: 2rem;
    font-family: "Playfair Display", serif;
    font-weight: 700;
    letter-spacing: -1px;
  }

  /* Hamburguesa Minimalista construida con CSS (Líneas finas) */
  .menu-trigger {
    display: flex;
    flex-direction: column;
    justify-content: center;
    gap: 6px;
    background: transparent;
    border: none;
    cursor: pointer;
    z-index: 110;
    width: 30px;
    height: 30px;
    padding: 0;
  }

  .menu-trigger .bar {
    width: 100%;
    height: 2px;
    background-color: var(--crema-suave, #f5f5f4);
    transition: transform 0.3s ease, background-color 0.3s ease;
  }

  /* Animación a "X" al abrir */
  .menu-trigger.open .bar:nth-child(1) {
    transform: translateY(4px) rotate(45deg);
    background-color: var(--dorado-guanerita, #d9c5a0);
  }
  .menu-trigger.open .bar:nth-child(2) {
    transform: translateY(-4px) rotate(-45deg);
    background-color: var(--dorado-guanerita, #d9c5a0);
  }

  /* ==========================================================================
     DISEÑO DEL MENÚ FULL SCREEN (Móvil por defecto)
     ========================================================================== */
  .user {
    position: fixed;
    top: 0;
    left: 0;
    width: 100vw;
    height: 100vh;
    z-index: 105;
    display: flex;
    visibility: hidden;
    transition: visibility 0s 0.5s;
  }

  .user.active {
    visibility: visible;
    transition: visibility 0s 0s;
  }

  /* Panel Izquierdo (Imagen) - Oculto en móviles pequeños */
  .menu-graphic-panel {
    width: 0%;
    height: 100%;
    position: relative;
    overflow: hidden;
    transition: width 0.5s cubic-bezier(0.76, 0, 0.24, 1);
    background-color: #0a0a0a;
  }

  .panel-img {
    width: 100vw;
    height: 100vh;
    object-fit: cover;
    filter: contrast(110%) brightness(40%) grayscale(100%);
    position: absolute;
    top: 0;
    left: 0;
  }

  .vignette {
    position: absolute;
    inset: 0;
    background: radial-gradient(circle at center, transparent 20%, rgba(0,0,0,0.8) 100%);
  }

  .panel-badge {
    position: absolute;
    bottom: 40px;
    left: 40px;
    font-family: "Montserrat", sans-serif;
    color: var(--dorado-guanerita, #d9c5a0);
    border: 1px solid rgba(217, 197, 160, 0.3);
    padding: 6px 12px;
    font-size: 0.7rem;
    letter-spacing: 2px;
    text-transform: uppercase;
  }

  /* Panel Derecho (Contenido) */
  .menu-content-panel {
    width: 100%;
    height: 100%;
    background-color: #141413; /* Un tono un poco más claro que el negro puro */
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    padding: 120px 40px 40px 40px;
    transform: translateX(100%);
    transition: transform 0.5s cubic-bezier(0.76, 0, 0.24, 1);
  }

  .user.active .menu-content-panel {
    transform: translateX(0);
  }

  .panel-header-mobile {
    font-family: "Montserrat", sans-serif;
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 3px;
    color: rgba(255,255,255,0.3);
  }

  .links-container {
    display: flex;
    flex-direction: column;
    gap: 25px;
  }

  /* Enlaces con efecto hover sofisticado */
  .nav-link-premium {
    font-family: "Playfair Display", serif;
    font-size: 2.5rem;
    color: var(--crema-suave, #f5f5f4);
    position: relative;
    width: fit-content;
    transition: transform 0.3s ease, color 0.3s ease;
  }

  .nav-link-premium:hover {
    color: var(--dorado-guanerita, #d9c5a0);
    transform: translateX(10px);
  }

  .divider-line {
    width: 40px;
    height: 1px;
    background-color: var(--dorado-guanerita, #d9c5a0);
    margin: 15px 0;
    opacity: 0.5;
  }

  .nav-actions-premium {
    display: flex;
    flex-direction: column;
    gap: 20px;
  }

  .cart-icon-premium {
    display: flex;
    align-items: center;
    gap: 12px;
    color: var(--crema-suave, #f5f5f4);
  }

  .cart-icon-premium img {
    width: 24px;
    height: 24px;
  }

  .cart-text {
    font-family: "Montserrat", sans-serif;
    font-size: 0.9rem;
  }

  .btn-gold-outline {
    font-family: "Montserrat", sans-serif;
    border: 1px solid var(--dorado-guanerita, #d9c5a0);
    color: var(--dorado-guanerita, #d9c5a0);
    padding: 12px 24px;
    text-align: center;
    font-size: 0.85rem;
    font-weight: 600;
    letter-spacing: 1px;
    text-transform: uppercase;
    transition: background-color 0.3s, color 0.3s;
  }

  .btn-gold-outline:hover {
    background-color: var(--dorado-guanerita, #d9c5a0);
    color: #141413;
  }

  .panel-footer-premium {
    display: flex;
    justify-content: space-between;
    font-family: "Montserrat", sans-serif;
    font-size: 0.7rem;
    color: rgba(255,255,255,0.2);
    text-transform: uppercase;
    letter-spacing: 1.5px;
  }

  /* ==========================================================================
     VISTA DESKTOP ESTÁTICA (Mayor a 768px)
     ========================================================================== */
  @media screen and (min-width: 768px) {
    .menu-trigger {
      display: none;
    }

    .user {
      position: static;
      width: auto;
      height: auto;
      visibility: visible;
      display: flex;
      background-color: transparent;
    }

    .menu-graphic-panel, .panel-header-mobile, .divider-line, .panel-footer-premium, .cart-text {
      display: none;
    }

    .menu-content-panel {
      position: static;
      width: auto;
      height: auto;
      background-color: transparent;
      padding: 0;
      transform: none;
      transition: none;
    }

    .links-container {
      flex-direction: row;
      align-items: center;
      gap: 30px;
    }

    .nav-link-premium {
      font-size: 1.1rem;
      font-family: "Montserrat", sans-serif;
    }

    .nav-link-premium:hover {
      transform: none;
    }

    .nav-actions-premium {
      flex-direction: row;
      align-items: center;
      gap: 20px;
    }

    .btn-gold-outline {
      padding: 6px 16px;
      font-size: 0.8rem;
    }
  }

  /* ==========================================================================
     ACTIVACIÓN DE CO-PANEL EN TABLETS Y MONITORES MEDIANOS (Menú abierto)
     ========================================================================== */
  @media screen and (max-width: 767px) and (min-width: 640px) {
    .user.active .menu-graphic-panel {
      width: 45%; /* La imagen toma el 45% */
    }
    .user.active .menu-content-panel {
      width: 55%; /* El texto toma el 55% para un look asimétrico de revista */
    }
  }

  /* En computadoras o pantallas anchas que fuercen el responsive por alguna razón */
  @media screen and (max-width: 1200px) and (min-width: 768px) {
    /* Mantiene comportamiento por si alteras el breakpoint principal */
  }
</style>